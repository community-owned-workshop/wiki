# COW Wiki

Documentation for the Community Owned Workshop initiative.

## Local preview

```bash
python -m pip install -r requirements.txt
mkdocs serve
```

The documentation source lives in `docs/`. GitHub Pages is built automatically from `main`.
